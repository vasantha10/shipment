# Copy this file to .env and adjust if the default ports conflict with something
# already running on your machine. Defaults match the assignment's recommended ports.

BACKEND_HOST_PORT=8080
FRONTEND_HOST_PORT=3000
DB_HOST_PORT=5432
